# 智翔館NEP マーケティング・エージェント

校門配布まわりのマーケティング業務（配布計画・実績分析・提案）を支援するエージェント。
このファイルは毎セッションの先頭に読み込まれる常時コンテキスト。簡潔に保つこと。

## 運用前提

- 操作するのはマーケティング担当者1名のみ。他の職員はこのエージェントを直接操作しない。
- エージェントの成果物（配布計画・実績分析・提案）は、Excel / Google Sheets で扱える形に整形して人に渡す。
- 認証は会社の Claude Console アカウント（商用規約）。個人アカウントは使わない。

## データ取り扱いの原則（最重要）

- `data/distribution.db`（SQLite）には**集計値のみ**を持つ：校舎別の在籍数・配布枚数・反応数・施策など。
- **個人情報（生徒氏名・連絡先など）はこのDBにも作業ディレクトリにも持ち込まない。** 反応は人数（カウント）で扱う。
- `data/raw/` は生データの一時置き場。原則ここに機密を残さず、集計後はDBに移して消す。`.gitignore` 済みで読み取りも `deny`。
- `.env` / 認証情報はコミットしない・読み取りしない。
- ネットワーク送信系コマンド（curl/wget 等）は `deny`。外部送信が必要なときは人が判断する。

## ディレクトリ構成

- `CLAUDE.md` … このファイル（運用ルール）
- `.claude/settings.json` … 権限（allow/deny）
- `.claude/commands/` … スラッシュコマンド（/配布計画 /実績分析 /提案作成）
- `.mcp.json` … MCPツール1号（haifu-tool）の配線
- `knowledge/` … ナレッジ層（Markdown、Obsidian vault としても閲覧可）
  - `校舎特性/` … 校舎ごとの立地・客層・配布の勘所
  - `過去施策/` … 過去の配布施策の振り返り
  - `提案テンプレ/` … 提案の雛形
- `data/` … 業務データ層（SQLite）。`schema.sql` 以外はコミットしない
- `mcp/` … MCPツール1号 のソース/ビルド成果物の置き場

## 対象の校舎と部門

- 校舎：駅前校 / 日野校 / 大野校 / 日宇校 / 大島校
- 部門：中等部 / RED個別

## ワークフロー

- `/配布計画 [校舎] [目標 or 在庫]` … MCPツール1号で配布数を算出し、校舎特性を踏まえた計画にする
- `/実績分析 [期間 or 校舎]` … distribution.db を集計し、配布枚数あたりの反応率・目標差を出す
- `/提案作成 [テーマ/校舎]` … 実績と提案テンプレから提案ドラフトを作る

## MCPツール1号（haifu-tool）

販促ティッシュ配布計画用の自作 MCP サーバー。2モード。

- target mode … 在籍数からの積み上げで必要配布数を算出（ボトムアップ）
- supply mode … 手元在庫を各校舎へ配分

反応率は `data/distribution.db` の過去実績（配布枚数あたりの反応数）から算出する。
実績が無い校舎は既定値にフォールバックし、その旨を出力に明示する。
呼び出し前に、対象校舎の `knowledge/校舎特性/` を読んで前提を揃えること。

## 分析の約束ごと

- 目標と実績は別物として扱う。矢印記法を使うときは「目標→実績」の向きを取り違えない。
- 反応率は必ず分母（配布枚数）を明示してから出す。
- 数字を作り話で埋めない。DBにデータが無ければ「データ無し」と明記する。

## セキュリティ注記

`deny` ルールは best-effort で、Bash の別経路では機密を読めてしまう余地がある。
より強い分離が必要なら、コンテナ/サンドボックス上で実行することを検討する。
